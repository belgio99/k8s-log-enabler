import subprocess

#def enableSidecarInjection(namespace):
#    print("Enabling automatic sidecar injection for namespace " + namespace)
#    subprocess.run(["kubectl label namespace", namespace, "istio-injection=enabled"], check=True)

